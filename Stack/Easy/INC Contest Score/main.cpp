#include <iostream>
#include <stack>

using namespace std;

int main()
{
    int t;
    cin >> t;

    while (t--)
    {
        int n;
        cin >> n;

        int *arr = new int[n];
        stack<int> st;

        for (int i = 0; i < n; i++)
        {
            cin >> arr[i];
        }

        int res[n];
        res[n - 1] = -1;

        for (int i = n - 1; i >= 0; i--)
        {
            while (st.)
        }
    }
}