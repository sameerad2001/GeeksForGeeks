#include <iostream>

using namespace std;

int maxSubarrayXOR(int A[], int n)
{
    int maximum = -1, temp = -1;
    for (int i = 0; i < n; i++)
    {
        
    }
}

int main()
{
    int n;
    cin >> n;
    int *arr = new int[n];

    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }

    delete[] arr;
}